/**
 * Based off of: http://www.cokeandcode.com/pathfinding
 * */

package info.eronarn.glyph.actor;

import info.eronarn.glyph.game.Game;
import info.eronarn.glyph.map.Pair;

public class Pathfinding {

	int width;
	int height;
	/**
	public int FindPath (Pair start, Pair finish) {
		if start.equals(finish) {
			return 0;
		}
		
		OpenList;
		ClosedList;
		
		private ArrayList closed = new ArrayList ();
		
		int[][] GCost = new int[Game.map.xmax][Game.map.ymax];
		GCost[start.X()][start.Y()] = 0;
		
		int[] OpenList = {1};
		
		
		
	}
	public
	**/
}
